apiVersion: v1
kind: Service
metadata:
  name: springboot-app-service
spec:
  type: NodePort
  selector:
    app: springboot-app
  ports:
    - port: 8082           # Service port changed to 8082
      targetPort: 8082     # Container port changed to 8082
      nodePort: 30082      # Different NodePort